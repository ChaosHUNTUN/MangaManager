# 2026-06-09

## 全项目优化实施 ✅ 完成

### 已完成的优化（6项全部完成，build 验证通过）：

1. ✅ **删除 Home.jsx** — 移除 456 行未使用遗留代码
2. ✅ **EHentai 阅读器提取** — 提取为 `components/EhentaiReader.jsx` (220行)，EHentai.jsx 从 1162→970 行
3. ✅ **LocalGallery.jsx 拆分** — 从 ~2400→1145 行：
   - 拖拽逻辑 → `hooks/useGalleryDrag.js` (160行)
   - 详情弹窗 → `components/GalleryDetail.jsx` (85行)
   - 专辑侧边栏 → `components/AlbumSidebar.jsx` (100行)
4. ✅ **骨架屏 + 弹窗适配** — CSS 骨架屏类 + 详情弹窗响应式 maxWidth
5. ✅ **阅读器进度加固** — 2秒防抖自动保存 + sendBeacon 关闭保护
6. ✅ **API 统一 + Toast 队列 + 快捷键提示** — api.js 统一用 request()，Toast 最多3条，阅读器顶栏 ? 按钮

### 文件变更：
- 删除: `pages/Home.jsx`
- 新增: `components/EhentaiReader.jsx`, `components/GalleryDetail.jsx`, `components/AlbumSidebar.jsx`, `hooks/useGalleryDrag.js`
- 修改: `pages/EHentai.jsx` (-192行), `pages/LocalGallery.jsx` (-1255行), `pages/ReaderLocal.jsx`, `pages/Reader.jsx`, `api.js`, `App.css`
- Build: ✓ 39 modules, 136ms, 111.50 KB gzipped JS

## 后续调整 (2026-06-10)
7. ✅ **移除补全元数据按钮** — 删除 `handleRepairMetadata`、`repairing`/`repairProgress` 状态、工具栏按钮和进度显示，清理未使用的 `API_BASE` import
