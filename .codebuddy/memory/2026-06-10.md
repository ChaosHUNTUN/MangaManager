# 2026-06-10

## 专辑名称罗马音→中文翻译
- 通过 `scripts/update_album_names.py` 直接更新 SQLite 数据库 `album_config` 表
- 更新了 18 个专辑的 Name 字段（罗马音→中文翻译）
- `jt dong-agyoku` 不在数据库中，跳过
- 翻译对照详见脚本文件

## 自动分组逻辑修复
- LocalGallery.jsx 分组逻辑：当 1 artist + 1 group 时，之前错误归类为"多作者"
- 修复：新增 `artists.length === 1 && grps.length === 1` 分支，优先用 artist 分组
- 自动匹配逻辑中 tags 数组 artist 在前，已天然优先匹配 artist Key，无需额外修改

## 空专辑自动隐藏
- 从自动分组转换的自定义专辑，内部作品全部清空后仍显示在列表中
- 修复：显示专辑前先收集所有画廊的 artist/group 名称，空专辑仅当其 Key 对应名称仍有未归类作品时才显示
- 手动创建的空专辑（Key 不与任何现有 artist/group 重合）在无作品时自动隐藏

## 自动分组 count=0 过滤
- 作品从磁盘删除后，自动分组（artist:xxx / group:xxx）仍显示 count=0 的空分组
- 修复：groups 计算结果过滤掉 count=0 的自动分组，只保留 count>0 的显示
- 自定义专辑（album）不受此过滤影响，仍按原有逻辑显示

## convertGroupToAlbum 过滤条件同步
- 之前修改了分组逻辑（1 artist + 1 group → artist分组），但 convertGroupToAlbum 的过滤条件未同步
- artist 分组转专辑时仍要求 `gr.length === 0`，导致 1 artist + 1 group 的作品被排除
- 结果：gids 为空 → `gids.length === 0` → 直接 return，看起来点击无反应
- 修复：artist 分支增加 `a.length===1 && a[0]===n && gr.length===1` 条件

## 本地画廊汉化搜索功能
- 本地画廊搜索不支持中文标签名（如搜索"汉化"无结果），而在线画廊通过 suggestEHTags 支持
- 修复分两部分：
  1. **自动补全**：handleSearchInput 中本地标签池结果不足时，fallback 调用 suggestEHTags API
  2. **搜索过滤**：galleries 加载后批量调用 translateEHTags 构建 searchTagTransMap，filtered useMemo 中同时匹配中文翻译
- 搜索词现在支持：原始标签名、中文翻译名、中文翻译名的部分匹配

## "添加到专辑"校验增强
- **GalleryDetail.jsx** 新增 props: `albumConfig`, `galleries`
- 新增 `useMemo` 计算 `inCustomAlbum`（作品是否已在自定义专辑中）、`matchedAlbums`（作品标签匹配的已有专辑列表）
- 已在自定义专辑中的作品隐藏"添加到专辑"按钮
- 按钮上显示匹配专辑数量提示（如 "(3 个匹配)"）
- **LocalGallery.jsx** 弹窗增强：
  - 顶部显示 🔗 匹配的专辑（标签属性一致），高亮优先选择
  - 下方显示所有已有专辑（匹配的以半透明显示避免重复）
  - "用标签创建新专辑"只显示 artist/group 标签作为关键标签属性
  - 支持选择关键标签 + 自定义显示名称创建新专辑
  - 移除了纯手动文本输入（必须从作品标签中选择一个作为Key）

## 移除 NeeView 阅读器集成
- 删除 `NeeViewService.cs`（服务类）和 `OpenController.cs`（API 控制器）
- 清理 `Program.cs` 中 `builder.Services.AddSingleton<NeeViewService>()` 注册
- 清理 `appsettings.json` 中 `NeeView:Path` 配置项
- 清理 `publish.ps1` 中 NeeView 配置生成和 README.txt 说明
- 清理前端 `Detail.jsx`：移除 `openInNeeView`/`pollReadingStatus` 导入、`handleOpen`/`startPolling` 函数、NeeView 按钮和状态消息
- 清理 `api.js`：移除 `openInNeeView()` 和 `pollReadingStatus()` 函数
- 清理 `test_api.py`：移除 NeeView 相关测试用例
- 更新 `README.md`：移除 NeeView 集成章节、API 表格中 NeeView 端点、技术栈和环境要求中的 NeeView 条目
- 阅读现仅依赖网页端阅读器（`/reader/:id`）
